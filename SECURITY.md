# AgentHub 安全文档

## 安全措施概述

AgentHub 在多个层面实施安全防护，确保平台运行安全可控。

## 输入安全

### SSRF 防护
- 所有外部 URL 请求经过白名单校验
- 禁止访问内网地址（127.0.0.1、10.x、172.16.x、192.168.x）

### 路径穿越防护
- 文件操作路径规范化处理
- 禁止 `..` 和符号链接逃逸

### MCP 命令注入防护
- 命令白名单机制
- 参数转义与校验

## 代码安全

### AST 沙盒
- 代码在受限沙盒中执行
- 禁止危险系统调用（os.system、subprocess 等）
- 资源限制：执行时间、内存占用

### Git 安全
- 路径校验：防止仓库外文件操作
- 命令白名单：仅允许 commit、push、create PR
- 超时控制：防止长时间阻塞

## 数据安全

### 加密模块
- 敏感配置（API Key）加密存储
- 传输层使用 HTTPS/WSS

### 技能库安全
- 滑动窗口限制防止内存膨胀
- 每个 Agent 最多保留 100 个技能
- 技能质量评估：基于 Quality Gate 分数过滤低质量技能

## 依赖安全

### 代码审查
- 规则引擎检测硬编码密码、SQL 注入、命令注入等安全问题
- LLM 深度审查代码正确性与安全性
- 自愈管道：高危问题自动触发修复重试

### CI/CD 安全
- Bandit 静态安全扫描
- ESLint 前端代码规范检查
- GitHub Actions 自动化流水线

## 配置安全

### LLM API Key 保护
- 前端设置面板配置，后端加密持久化
- 日志输出中自动脱敏
- 无 API Key 时降级为 Mock 回复，不暴露敏感信息
