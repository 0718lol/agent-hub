[
  {
    "tool": "alignment_judge",
    "description": "高对齐 — 实现完全匹配需求",
    "task": "用 Python 写一个函数，输入一个整数列表，返回所有偶数的和",
    "solution": "```python\ndef sum_of_evens(numbers: list[int]) -> int:\n    return sum(n for n in numbers if n % 2 == 0)\n```",
    "expected_decision": "aligned",
    "expected_score_range": [80, 100],
    "pass_threshold": 50
  },
  {
    "tool": "alignment_judge",
    "description": "技术栈偏离 — 用了错误的框架",
    "task": "用 React 实现一个登录表单",
    "solution": "```vue\n<template>\n  <form @submit=\"handleLogin\">\n    <input v-model=\"username\" />\n    <input v-model=\"password\" type=\"password\" />\n    <button>登录</button>\n  </form>\n</template>\n```",
    "expected_decision": "misaligned",
    "expected_score_range": [0, 50],
    "pass_threshold": 50
  },
  {
    "tool": "alignment_judge",
    "description": "功能缺失 — 只实现了一半需求",
    "task": "实现一个用户注册接口，需要：1) 邮箱格式校验 2) 密码强度校验 3) 写入数据库 4) 发送欢迎邮件",
    "solution": "```python\ndef register(email: str, password: str):\n    user = {'email': email, 'password': password}\n    db.users.insert(user)\n    return user\n```",
    "expected_decision": "misaligned",
    "expected_score_range": [0, 60],
    "pass_threshold": 50
  },
  {
    "tool": "alignment_judge",
    "description": "完全实现 — 复杂需求",
    "task": "用 Python FastAPI 实现一个 GET /users/{id} 接口，返回用户信息，找不到返回 404",
    "solution": "```python\nfrom fastapi import FastAPI, HTTPException\n\napp = FastAPI()\n\nusers_db = {1: {'id': 1, 'name': 'Alice'}}\n\n@app.get('/users/{user_id}')\nasync def get_user(user_id: int):\n    user = users_db.get(user_id)\n    if not user:\n        raise HTTPException(status_code=404, detail='User not found')\n    return user\n```",
    "expected_decision": "aligned",
    "expected_score_range": [80, 100],
    "pass_threshold": 50
  }
]
